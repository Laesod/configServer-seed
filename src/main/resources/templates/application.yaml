server:
  port: 8888
logging:
  file: myapplication.log
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: toBeReplacedByRepositoryUrl 
          username: toBeReplacedByUserName
          password: toBeReplacedByUserPassword

