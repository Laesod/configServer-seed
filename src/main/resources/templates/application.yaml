server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://aautushka@bitbucket.org/aautushka/tasksmanager-configuration.git
          username: toBeReplacedByUserName
          password: toBeReplacedByUserPassword

